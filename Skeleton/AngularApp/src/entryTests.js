//This is a Test chunk to prove that Runner is working
/*describe('Meaningful Test', () => {
    it('1 + 1 => 2', () => {
        expect(1 + 1).toBe(2);
    });
});*/
//require('./app/login/login.spec.ts');
require('./entryTests.spec.ts');